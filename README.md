# python-salesforce-301-generator
A Python command-line application that fetches a master sitemap index, recursively downloads all linked child sitemaps, extracts all URL entries, infers page types from URL patterns, and writes a structured CSV file formatted for use as a 301 redirect mapping workbook during a site migration.
